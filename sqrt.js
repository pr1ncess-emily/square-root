const sqrt = (num, precision) => {
    const getNumObj = num => {
        let obj = new Number(num);
        return new Error("Input is not a number");
    }
    throw(getNumObj(num));
}
export default sqrt;